---
name: plan
description: 用于写作和执行 gaokao 的单文件 plan.md。适用于中大型开发、跨模块重构、生产级验收、架构硬化、评测闭环，以及任何不能靠一次小补丁完成的任务；要求把 plan.md 写成需求、事实、失败测试、目标、设计、任务、验证和收口合一的规格驱动执行合同。
---

# Plan

`plan.md` 是单文件规格驱动执行合同。

它同时承担：

- 需求文档。
- 当前事实。
- 失败测试。
- 目标合同。
- 设计合同。
- 实施任务。
- 验证计划。
- 收口记录。

没有完整 `plan.md`，不做大改。

## 写作顺序

### 1. 需求文档

写用户能理解的非技术说明。

### 2. 当前事实

写已经确认的仓库事实和命令事实。

### 3. 失败测试

先写什么会失败。

### 4. 目标

写最终要交付的可验收结果。

### 5. 不做范围

写清楚当前任务边界。

### 6. 设计

写技术设计，覆盖主链路、模块边界、状态归属、数据流、错误边界和验证方式。

### 7. 实施任务

写可执行 checklist。

### 8. 验证计划

写最后怎么证明完成。

### 9. 收口

任务结束时更新完成事实、验证结果、未验证内容和剩余风险。

## 执行规则

- 写完计划，再动手。
- checklist 必须随进度更新。
- 新事实推翻计划，先改计划，再继续。
- 不用口头解释替代计划更新。
- 不偷偷偏离计划。
- research、设计、实现、测试、文档同步、验证和收口必须形成完整交付。
- 分阶段只表示交付边界，不表示降低标准；每个阶段都必须写清并达到当期生产级封顶验收。
- 阶段交付物必须能作为长期主干保留，不能依赖后续返工来补齐基础质量。
- 阶段编号只允许存在于 `roadmap.md` 和 `plan.md` 的交付管理语境；运行时代码、变量、类名、函数名、接口、页面文案、脚本和数据库文件不得使用 `V0`、`V1`、`V2` 这类阶段/版本占位命名。
- 数据库迁移文件必须使用业务语义命名；如果工具需要版本号，只能使用配置化的语义前缀和时间/序列号，不使用 `V` 前缀。
